#!/bin/bash

source ./functions
log_begin_msg

destdir=${SQUASHFSROOTDIR}; mkdir -p $destdir

# FIXME: Copy to /lib/ddcos/init.sh

# Root filesystem hierarchy
for dir in \
  bin \
  dev \
  etc \
  lib \
  lib/ddcos \
  proc \
  root \
  run \
  sbin \
  sys \
  tmp \
  usr \
  usr/bin \
  usr/lib \
  usr/sbin \
  var \
  var/run \
  var/log \
  var/lib \
; do
  mkdir -p ${destdir}/$dir
done

# System libraries
for lib in \
  /lib64 \
  /lib/klibc-* \
  /lib/firmware \
  /lib/init \
  /lib/lsb \
  /lib/modules \
  /lib/startpar \
  /lib/systemd \
  /lib/sysvinit \
  /lib/terminfo \
  /lib/udev \
  /lib/$(uname -m)-linux-gnu \
  /lib/xtables \
  /usr/lib/systemd \
; do
  cp -vax $lib ${destdir}$(dirname $lib)
done
cp -v /lib/ddcos/init.sh $destdir/lib/ddcos

# System binaries
ln -sf /bin/bash ${destdir}/bin/sh
ln -sf /bin/systemctl ${destdir}/sbin/telinit
ln -sf /lib/systemd/systemd ${destdir}/bin
ln -sf /lib/systemd/systemd ${destdir}/sbin/init
ln -sf /lib/systemd/systemd-bus-proxyd ${destdir}/usr/bin/systemd-stdio-bridge
ln -sf /lib/systemd/systemd-udevd ${destdir}/sbin/udevd
ln -sf /usr/bin/touch ${destdir}/bin

copy_binary_to_rootfs ${destdir} \
  agetty \
  auplink \
  awk \
  basename \
  bash \
  chgrp \
  clear \
  cp \
  dd \
  docker \
  echo \
  egrep \
  find \
  grep \
  head \
  id \
  ifconfig \
  ip \
  ip6tables \
  ip6tables-save \
  iptables \
  iptables-save \
  ls \
  mkdir \
  modprobe \
  mount \
  mountpoint \
  printf \
  ps \
  rm \
  rmdir \
  sed \
  start-stop-daemon \
  sysctl \
  systemctl \
  systemd-ask-password \
  systemd-analyze \
  systemd-cat \
  systemd-cgls \
  systemd-cgtop \
  systemd-delta \
  systemd-detect-virt \
  systemd-escape \
  systemd-inhibit \
  systemd-machine-id-setup \
  systemd-nspawn \
  systemd-notify \
  systemd-path \
  systemd-run \
  systemd-tmpfiles \
  systemd-tty-ask-password-agent \
  tail \
  test \
  touch \
  ulimit \
  umask \
  umount \
  xz

# Binaries from klibc-utils
for bin in \
  cat \
  dmesg \
  false \
  gunzip \
  gzip \
  ipconfig \
  kill \
  ln \
  mv \
  reboot \
  sleep \
  sync \
  true \
  uname \
  zcat \
; do
  cp -v /usr/lib/klibc/bin/$bin ${destdir}/bin
done

# Scripts from live-boot
cp -v /bin/live-* ${destdir}/bin

# Configuration files
printf '/lib/ddcos/docker-monitor.sh\n' >> /etc/shells
#useradd -g docker -M -s /lib/ddcos/docker-monitor.sh docker
useradd -g docker -d /var/run docker
printf 'DOCKER_OPTS=" \
  -s aufs \
  --host tcp://0.0.0.0:2375 \
  --host unix:///var/run/docker.sock \
"\n' >> /etc/default/docker

for conf in \
  default \
  fstab \
  group \
  gshadow \
  hostname \
  hosts \
  init \
  init.d \
  ld.so.cache \
  ld.so.conf \
  ld.so.conf.d \
  localtime \
  network \
  nsswitch.conf \
  passwd \
  protocols \
  rc.local \
  resolv.conf \
  security \
  selinux \
  services \
  shadow \
  shells \
  ssl \
  sysctl.conf \
  sysctl.d \
  systemd \
; do
  cp -vax /etc/$conf ${destdir}/etc
done

touch ${destdir}/etc/resolv.conf
echo $DISTVER > ${destdir}/etc/version
echo $PUBLISHER > ${destdir}/etc/publisher

log_end_msg
