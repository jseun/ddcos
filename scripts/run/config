#!/bin/bash

source /scripts/config

ISODIR=${DATADIR}/iso
ISOFILE=${DATADIR}/${DISTVER}.iso
SQUASHFSDIR=${DATADIR}/squashfs
SQUASHFSROOTDIR=${SQUASHFSDIR}/rootfs
export ISODIR ISOFILE SQUASHFSDIR SQUASHFSROOTDIR
